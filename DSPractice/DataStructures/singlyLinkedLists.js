// A linked list is a list of data. It contains a train car structure with
// head, tail, and length attributes. There are no indices, instead each element is 
// called a node. 

// A linked list consist of nodes each node has a value and a pointer to the next node.
// Random access not allowed. 

